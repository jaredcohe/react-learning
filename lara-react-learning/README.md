open http://127.0.0.1:8000 && python -m SimpleHTTPServer

var Avatar = React.createClass({
  
  render: function() {

    return (
      
      <div>
    
    
        Hello world
      
      </div>
      
    );

  }
  
});

React.render(<Avatar />, document.body)

# to 8